interface:
  display_name: "Mermail Bounty Settler"
  short_description: "Automate bounty delivery tracking, verification email handling, and on-chain settlement reconciliation."
  default_prompt: "Use $mermail-bounty-settler to monitor incoming bounty communications and reconcile treasury settlements."
dependencies:
  tools:
    - type: "mcp"
      value: "mermail"
      description: "Mermail workspace and mailbox MCP server"
      transport: "streamable_http"
      url: "https://console.mermail.app/mcp"
